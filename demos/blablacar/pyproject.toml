[project]
name = "blablacar-demo"
version = "0.1.0"
description = "BlaBlaCar voice customer support agent with trip verification, lost items, and refund requests"
requires-python = ">=3.12"
dependencies = ["gradbot"]

[tool.uv.sources]
gradbot = { path = "../../gradbot_py" }
