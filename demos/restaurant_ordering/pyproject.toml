[project]
name = "restaurant-ordering-demo"
version = "0.1.0"
description = "Voice AI fast-food restaurant ordering demo"
requires-python = ">=3.12"
dependencies = ["gradbot"]

[tool.uv.sources]
gradbot = { path = "../../gradbot_py" }
