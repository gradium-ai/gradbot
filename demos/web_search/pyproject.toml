[project]
name = "web-search-demo"
version = "0.1.0"
description = "Voice-powered web search assistant using DuckDuckGo"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115.0",
    "uvicorn[standard]>=0.30.0",
    "pygradbot",
    "pyyaml>=6.0",
    "ddgs",
]

[tool.uv.sources]
pygradbot = { path = "../../pygradbot" }
