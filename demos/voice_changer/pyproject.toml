[project]
name = "voice-changer-demo"
version = "0.1.0"
description = "Voice changer demo where the AI can switch between voices"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115.0",
    "uvicorn[standard]>=0.30.0",
    "gradbot",
    "pyyaml>=6.0",
]

[tool.uv.sources]
gradbot = { path = "../../gradbot_py" }
